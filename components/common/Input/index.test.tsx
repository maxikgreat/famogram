import {render, fireEvent, queryByTestId} from '@testing-library/react';import faker from 'faker';import {faInstagram} from '@fortawesome/free-brands-svg-icons';import {Input} from '..';describe('Input component' , () => {  it('should change value input with the most of props', () => {    const {queryByPlaceholderText, queryByTestId} = render(      <Input        icon={faInstagram}        placeholder="test"        label={faker.random.words(5)}        error={faker.random.words(5)}      />    );        const input = queryByPlaceholderText('test') as HTMLInputElement;    const icon = queryByTestId('test-icon') as HTMLLabelElement;    const label = queryByTestId('test-label') as HTMLParagraphElement;    const error = queryByTestId('test-error') as HTMLDivElement;      fireEvent.change(input, {target: {value: 10}});    expect(input.value).toBe('10');        expect(icon).toBeTruthy();    expect(label).toBeTruthy();    expect(error).toBeTruthy();  });});