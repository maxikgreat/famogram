import {render, RenderResult} from '@testing-library/react';import {Logo} from '.';describe('Logo component', () => {  let logo: RenderResult;    it('should render desktop logo and match snapshot', () => {    logo = render(<Logo />);        expect(logo.queryByTestId('logo-desktop')).toBeTruthy();    expect(logo.queryByTestId('logo-mobile')).not.toBeTruthy();    expect(logo.queryByTestId('logo-small')).not.toBeTruthy();        expect(logo).toMatchSnapshot();  });    it('should render mobile logo', () => {    logo = render(<Logo isMobile={true} />);        expect(logo.queryByTestId('logo-mobile')).toBeTruthy();    expect(logo.queryByTestId('logo-desktop')).not.toBeTruthy();    expect(logo.queryByTestId('logo-small')).not.toBeTruthy();  });    it('should render small logo', () => {    logo = render(<Logo type="text" />);      expect(logo.queryByTestId('logo-small')).toBeTruthy();    expect(logo.queryByTestId('logo-mobile')).not.toBeTruthy();    expect(logo.queryByTestId('logo-desktop')).not.toBeTruthy();  })});