import {render} from '@testing-library/react';import faker from 'faker';import Router from 'next/router';import {Redirect} from '.';describe('Redirect component', () => {  let startUrl = faker.internet.url();    jest.spyOn(Router, 'push')    // @ts-ignore    .mockImplementation((url: string) => startUrl = url);    it('should redirect with url', () => {    const destinationUrl = faker.internet.url();    const {container} = render(<Redirect url={destinationUrl} />);        expect(startUrl).toEqual(destinationUrl);    expect(container.childElementCount).toBe(0);  });});