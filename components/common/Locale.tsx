import {VFC} from 'react';import Link from 'next/link';import useTranslation from 'next-translate/useTranslation'import {useRouter} from 'next/router';import i18nConfig from '@/i18n.json'const { locales } = i18nConfig;export const Locale: VFC = () => {	const { lang } = useTranslation()	const { asPath } = useRouter();	return (		<div className="dropup position-absolute langs px-2 py-1">			<a href="#" role="button" id="dropup" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">				<span className="text-primary">{lang}</span>			</a>			<ul className="dropdown-menu" aria-labelledby="dropup">				{locales?.map(localeItem => localeItem !== lang && (					<li className="text-center my-1" key={localeItem}>						<Link href={asPath} locale={localeItem}>							<a>{localeItem}</a>						</Link>					</li>				))}			</ul>		</div>	)}