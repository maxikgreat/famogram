import {NextPageContext} from 'next';export function withAuth(	callback?: (...args: any) => any,) {	return async ({ query }: NextPageContext) => {		const {user, token} = query;		if (!user || !token) {			return {				redirect: {					destination: '/login',					permanent: false				}			}		}		return {			props: {				user: JSON.parse(user as string),				token,			}		}	}}